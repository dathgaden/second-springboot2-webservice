language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

#gradlew에 실행권한 설정
before_install:
  - chomod +x gradlew

#CI 실행 완료시 메일로 전달
notifications:
  email:
    recipients:
      - kswon12@naver.com